<Window x:Class="EDActivityOverlay.Windows.VrCompositeOverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:EDActivityOverlay.UserControls"
        Title="EDAO VR | Composite"
        Width="1" Height="1"
        WindowStyle="None"
        ResizeMode="NoResize"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="True"
        ShowActivated="False"
        Focusable="False"
        Topmost="True"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True">
    <Canvas x:Name="OverlayCanvas"
            Background="Transparent"
            ClipToBounds="True"
            IsHitTestVisible="False">
        <controls:ShipStatusPanelControl x:Name="ShipStatusPanel" />
        <controls:NotificationPanelControl x:Name="NotificationPanel" />
        <controls:PinnedRoutePanelControl x:Name="PinnedRoutePanel" />
    </Canvas>
</Window>
